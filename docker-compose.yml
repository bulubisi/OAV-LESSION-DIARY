services:
  oav-lesson-planner:
    build: .
    container_name: oav-lesson-planner
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE
    restart: unless-stopped
